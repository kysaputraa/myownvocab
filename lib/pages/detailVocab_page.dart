import 'package:flutter/material.dart';

class DetailVocabPage extends StatelessWidget {
  const DetailVocabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('asd'),
    );
  }
}
